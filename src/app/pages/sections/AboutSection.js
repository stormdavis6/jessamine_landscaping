import styles from '../../styles/Home.module.css';

export default function AboutSection() {
    return (
        <section className={styles.about}>
            <h2>Our Story</h2>
            <p>Jessamine Landscaping is named after South Carolina’s state flower...</p>
        </section>
    );
}
